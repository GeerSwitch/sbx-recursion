Clone, `cd`, and `npm install` like normal.

Change the `directory` variable in recurse.js to a folder that doesn't have too
many subdirectories.  This is just a small example of recursion with comments.